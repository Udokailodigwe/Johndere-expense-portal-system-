version: "3.9"

services:
  backend:
    build: ./server
    env_file:
      - ./server/.env
    environment:
      - NODE_ENV=production
    command: npm start

  # frontend:
  #   build:
  #     context: ./client
  #     dockerfile: Dockerfile
  #   environment:
  #     - NODE_ENV=production
  #   depends_on:
  #     - backend
  #   #command: npm run build

  nginx:
    build:
      context: .
      dockerfile: ./client/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
